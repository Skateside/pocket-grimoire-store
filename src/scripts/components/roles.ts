import type {
    IAppGetSlice,
} from "../types/app";
import {
    findOrDie,
    renderTemplate,
} from "../utilities/dom";

export default function roles(getSlice: IAppGetSlice) {

    const rolesSlice = getSlice("roles");


}

// const 
